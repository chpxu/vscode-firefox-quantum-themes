package size
package main

package	fmt

func Size(a int) string {
	switch {
		case a < 0:
			return "negative"
	}
}
 
func main() {
	var numOne, numTwo int
	fmt.Print("Enter num ")
	fmt.Scan(&numOne)
}

for i := 0; i < f.Len(); i++ {
	if i > 0 {
		p.buf.Write(commaSpaceBytes)
	}
}
